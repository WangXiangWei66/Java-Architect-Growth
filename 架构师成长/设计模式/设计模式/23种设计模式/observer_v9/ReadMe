javascript event:发生事件时由浏览器自动创建的对象

Event 对象代表了某个事件的状态和相关数据
当事件触发时（如点击、键盘输入、页面加载），浏览器会创建一个 Event 对象
该对象会自动传递给事件处理函数作为参数
可通过它获取事件类型、目标元素、鼠标位置等信息

在很多系统中，Observer模式往往和责任链共同负责对于事件的处理，
其中的某一个observer负责是否将事件进一步传递